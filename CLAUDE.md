# CLAUDE.md

@SKILL.md

Guidance for Claude Code working in this repo. Per-module deep dives live in `src/<module>/` and `docs/`; this file captures only what can't be derived by reading the code.

## Model Routing & Orchestration (Fable 5 workflow)

Fable 5 drives this repo. It is expensive — delegate token-heavy grunt work to
cheaper models via the shell-out skills below; keep taste- and judgment-heavy
work on Fable/Opus. Reasoning effort stays **high** — never x-high/max/ultracode
for routine work (it overthinks, overbuilds, and costs multiples for marginal gain).

**Glossary** (evaluation axes used throughout):
- **Intelligence** — how hard a problem the model handles unsupervised
- **Taste** — UI/UX, code quality, API design, copy
- **Cost** — effective cost given active subscriptions (Claude Code + Codex CLI)

**Routing table** (1–10; cost 10 = cheapest):

| Model | Cost | Intelligence | Taste | Default use |
|---|---|---|---|---|
| GPT via `codex exec` | 9 | 8 | 4 | Bulk mechanical work: clear-spec implementations, migrations, log digging, big-file reading, independent second-opinion reviews |
| Sonnet 5 | 5 | 6 | 5 | Sub-agent fan-out; thin proxy wrapper around external CLIs inside workflows (low effort) |
| Opus 4.8 | 6 | 8 | 8 | Reviews and plans when Fable budget matters |
| Fable 5 | 3 | 10 | 10 | Orchestration, architecture, API design, final review, anything user-facing |
| Haiku 4.5 | — | skip | — | Not for real work |

**Routing rules:**
- These are defaults, not limits. If a cheaper model's output misses the bar,
  rerun with a smarter model **without asking**. Judge the output, not the price tag.
- Use cheap models to gather information and try things before moving work to
  an expensive one; never let cost pick the wrong model for the final pass.
- Bulk/mechanical → codex (see `external-review` / `external-implementation`
  skills). Taste- or user-facing → Fable or Opus.
- Reviews of plans and implementations: Fable/Opus primary; codex as an extra
  independent perspective.
- Prompting codex: keep prompts much simpler than for Claude — one or two
  sentences of task + focus. Don't prompt it like it's Claude.

**Orchestration rules:**
- **Subagents do NOT compile, test, or commit. The orchestrator does.**
  Concurrent `cargo build`/`cargo test` across N worktrees saturates the
  CPU — 5 parallel builds means ~62 rustc processes fighting for cores, so
  a 3-minute test run takes 30+ and nothing finishes. A spawned
  implementation agent's job is to **write the change and report what it
  wrote** (files touched, intent, anything uncertain). It must not run
  `cargo build`/`clippy`/`test` or `git commit`/`push`. The orchestrator
  gathers all agents' findings, then serially — one cargo invocation at a
  time — compiles, tests, and commits each branch. If an agent needs a
  quick `cargo check` to validate a tricky change, that is the exception,
  not the default, and never in parallel with sibling agents.
- Don't pre-define agent archetypes — invent the right reviewers/implementers
  per task; every review has different needs.
- Workflows can only spawn Claude models. To use codex inside a workflow,
  spawn Sonnet at low effort as a thin wrapper that shells out to the codex
  CLI and reports back; label those sub-agents with a `codex-proxy:` prefix.
- Checkpoint-driven sequences (CI must pass → review → merge → rebase next)
  are orchestrated from the main session with worktrees — not one giant
  workflow, which barrels past checkpoints or stalls. Use workflows for what
  they're strong at: multi-agent review fan-out before a merge.
- Time-to-complete is an architecture signal on ad-hoc fixes: <3 min = low-risk;
  ~15 min = look closer before merging; 1 h+ = the architecture in that area is
  the problem — go deeper, don't merge blind.

## Build Commands

```bash
cargo build                                              # Debug
cargo build --release                                    # Release (ships WITHOUT test-bypass — bypass env vars are inert)
cargo fmt --all                                          # Format
cargo clippy --all-features -- -D warnings               # Lint (CI gate)
cargo check --verbose                                    # Type check
cargo test --all-features --verbose -- --show-output     # All tests (CI uses --all-features; equivalent to --features test-bypass)
cargo test --features test-bypass                        # Local-dev shorthand for the test-bypass-gated integration tests
cargo test --test cli_dispatch -- --show-output          # Single integration test
cargo run -p cargo-jarvy -- new-tool <name>              # Scaffold tool
```

**`test-bypass` feature.** Several integration tests use loopback HTTP
(`JARVY_LIBRARY_ALLOW_INSECURE_FETCH`, `JARVY_REGISTRY_ALLOW_INSECURE_FETCH`)
or a redirected home (`JARVY_TEST_HOME`). Those escape hatches are
compiled out of release builds (review item 15) — the env vars are
inert in shipped binaries. `cargo test` without `--features test-bypass`
silently skips the dependent integration tests (via `required-features`
in `Cargo.toml`); CI invocations pass `--all-features` so the gate is
automatic.

## Architecture

Cross-platform CLI that provisions dev environments from `jarvy.toml` using native package managers (brew on macOS, apt/dnf/etc on Linux, winget/choco/scoop on Windows).

### Module Map

- `src/main.rs` — minimal entry (~270 lines): args, telemetry init, OTLP flush. Dispatch + per-command glue live in `commands/dispatch.rs` (PRD-037).
- `src/cli/` — `args.rs` (`Cli`, `Commands`, `OutputFormat`), `subcommands.rs` (nested enums)
- `src/commands/` — one file per handler (`setup_cmd.rs`, `tools_cmd.rs`, `roles_cmd.rs`, `ai_hooks_cmd.rs`, `mcp_register_cmd.rs`, `drift_cmd.rs`, `logs_cmd.rs`, `ticket_cmd.rs`, `run_cmd.rs`, …) plus `dispatch.rs` (CLI routing table). `run_cmd.rs` = `jarvy run [name] [-- args]`, the npm-run-style runner for `[commands]`: explicit name = consent (chaining metachars allowed, command printed ANSI-stripped before exec, child exit code propagated, cwd = the config file's directory) — deliberately looser than the interactive menu's `classify_shell_command` gauntlet, NO implicit `cargo run`/`cargo test` fallback; NUL and (Windows) `%`-bearing `--` args refused. npm-style lifecycle hooks via `resolve_hook`: `pre<name>`/`post<name>` AND the colon spelling `"pre:<name>"`/`"post:<name>"` (colon wins when both defined, note printed); failing pre aborts the main, post runs only after success, `--` args reach the main command only — each hook executes through `execute_one` with its own `run.command.*` label. `jarvy shell-init` snippet defines `jr` as shorthand; `jarvy shell-init --apply` writes the loader line into the shell rc idempotently (nushell gets a materialized `~/.jarvy/init.nu`). `commands/shared.rs` holds the mechanics both surfaces share (`sanitize_for_display`, `spawn_shell` incl. Windows `cmd /C`, `quote_shell_arg`, `short_cmd_hash`); the `[commands]` loader + Trojan-Source key sanitizer live in `config.rs::read_commands_config` (policy stays per-caller: menu defaults on error, `jarvy run` hard-errors).
- `src/config.rs` — `jarvy.toml` parser. **`TOP_LEVEL_SECTIONS` const is canonical**; `validate.rs` consumes it. Compile-time destructure test `config::tests::top_level_sections_matches_config_fields` rejects adding a `Config` field without updating both.
- `src/tools/` — `registry.rs` (global `OnceLock<RwLock<HashMap>>`), `common.rs` (`Os`, `InstallError`, `run`, `has`, `cmd_satisfies`, `is_wsl`), `spec.rs` (`ToolSpec` + `define_tool!` macro)
- `src/packages/` — npm / pip / cargo / nuget / gem / go handlers
- `src/roles/` — role-based config with inheritance (PRD-033)
- `src/agents.rs` — canonical `Agent` enum shared across `ai_hooks`, `mcp_register`, and `skills`. The three former per-subsystem enums (`AgentTarget`, `McpAgentTarget`, `SkillAgent`) are now `pub use crate::agents::Agent as <Name>` aliases so call sites compile unchanged; a new agent variant lands in all three subsystems atomically.
- `src/ai_hooks/` — guardrail hooks distributed to Claude Code, Cursor, Codex, Windsurf, Cline, Continue. See `docs/ai-hooks.md`.
- `src/mcp_register/` — auto-registers Jarvy MCP server with the same 6 agents. See `docs/mcp-registration.md`.
- `src/mcp/extended_tools.rs` — broader Jarvy surface exposed over MCP (drift / roles / templates / services). Mutating tools gated by `gate_mutation` + `MutationCtx` (rate limit → stderr TTY confirm → audit log). Workspace containment via `safety::resolve_within_workspace` (canonical-root check, refuses `..`, absolute escapes, endpoint symlinks). Workspace root = `JARVY_MCP_WORKSPACE` env or cwd.
- `src/drift/` — state baseline + version policy (Major/Minor/Patch/Exact) + JSON/human reporter
- `src/network/` — proxy + CA bundle resolution. Priority: env > tool override > global. Propagates `HTTP(S)_PROXY`, `NO_PROXY`, `CURL_CA_BUNDLE`, `SSL_CERT_FILE`, `NODE_EXTRA_CA_CERTS`, `GIT_SSL_CAINFO`.
- `src/net/` — shared HTTP primitives. `agent` (ureq config + UA), `bounded_fetch` (HTTPS-only refusal, bounded read, loopback-bypass parser — used by both `library_registry::fetch` and `registry_remote::fetch`; each consumer passes its own env-var name so test isolation stays per-subsystem), `url_encode`.
- `src/git/` — git config automation (identity, signing, aliases). `ConfigValue` = plain string or `{ env, default }`. Signing format auto-detected (`.pub` → ssh, else gpg). Credential helper defaults per-OS. `[git.extra]` is a free-form escape hatch (`HashMap<key,value>`) for un-modeled keys, applied last so they override typed fields. **Remote-config trust gate** (`run_git_phase`): a `ConfigOrigin::Remote` config cannot apply `[git]` unless `[git] allow_remote = true`, and even then writes are forced to `--local` scope (mirrors `[git_hooks] allow_remote`; without it a remote could write `~/.gitconfig`). `[git.extra]` gauntlet is `check_extra_entry` (shared by `configure_extra` write-path AND `extra_write_plan`, which the dry-run preview consumes so preview == apply): `validate_extra_key` (dotted grammar + flag-injection, ≤256B) → leading-`-` value refusal (argv option-injection) → `is_exec_capable_key` refuses keys whose value git *executes* (`core.pager`/`sshCommand`/`hooksPath`/`askPass`/`fsmonitor`(non-bool)/`pager.*`/`merge.*.driver`/`remote.*.uploadpack`/`init.templateDir`/`filter.*.clean`/`*.textconv`/… — RCE the `!` filter misses) unless `JARVY_ALLOW_GIT_EXEC_KEYS=1` (`exec_key_value_is_safe` exempts `core.fsmonitor=true|false`) → `check_not_protect_downgrade` (protectNTFS/HFS=false, `safe.directory=*`/`safe.bareRepository=all`, `fsck.*`/`fetch.fsck.*`/`receive.fsck.*=ignore`, `*.fsckObjects=false`, `*.sslVerify=false`; `JARVY_ALLOW_GIT_PROTECT_DOWNGRADE=1` override, both refusal + override emit events) → `value_is_shell_escape` (`!` after trim). **Typed `editor`/`credential_helper` fields** funnel through `set_config`, which guards the shell-interpreted keys by VALUE (`value_has_shell_metachars` / `credential_helper_is_program_path`) — a bare command + flags is allowed, injection/path is refused (the RCE the `[git.extra]` denylist doesn't reach). `os_defaults` (default on; `= false` opts out) applies `os_default_plan()` (pure) for unset keys — autocrlf/longpaths(Win)/precomposeunicode(mac) + recommended `fetch.prune`/`rerere.enabled`/`merge.conflictStyle=zdiff3`; `os_defaults_to_write` + `parse_null_config` skip already-matching keys (one `existing_config()` read). All env opt-ins parse via `env_flag_enabled`; ALL `git_config.*` events go through the `gated_*!` macros. See `docs/git-config.md`.
- `src/git_hooks/` — native git-hook management (PRD-048). `[git_hooks]` block in `jarvy.toml`. Auto-install during `jarvy setup` between git-config and ai-hooks phases. Pre-commit framework support was removed in v0.8 — jarvy no longer shells out to `pre-commit install`, no `.pre-commit-config.yaml`, no third-party dependency. Husky / lefthook still auto-detected but handlers stubbed (`UnsupportedFramework` error). Remote-config trust gate via `[git_hooks] allow_remote`. **Native handler** (`[git_hooks.native]`) writes scripts directly into `.git/hooks/<stage>` — covers all 23 git hook stages (`pre-commit`, `pre-push`, `commit-msg`, `post-checkout`, `pre-rebase`, `applypatch-msg`, …). FOUR source shapes teams can mix, later shapes overriding earlier for the same stage: remote repo (`repo = "github:org/team-hooks"` + `ref = "v1.0.0"` + optional `subpath = "hooks"` — cloned to `~/.jarvy/git_hooks_cache/<sha256(repo+ref)>/` via `src/git_hooks/repo.rs`, refreshed idempotently on rerun with `git fetch + reset --hard <ref>`), local folder scan (`dir = "scripts/hooks"`), file reference (`hooks.pre-commit = { file = "scripts/hooks/pre-commit.sh" }`), or inline body (`hooks.pre-commit = "..."`). Repo URLs support `github:owner/repo`, `https://...`, and `git+https://...` (mirror of `library_registry` grammar); `ref` is REQUIRED — unpinned URLs refused at parse time. Branch refs emit `git_hooks.mutable_ref` warning; SHAs and `v`-prefixed tags are treated as pinned. `git+file://` is refused unless `JARVY_LIBRARY_ALLOW_INSECURE_GIT=1` (e2e-test-only bypass). File paths refused if absolute, contain `..`, or canonicalize outside the project root (symlink escape blocked); repo subpaths refused if absolute or contain `..`. Explicit `hooks` map wins over `dir` which wins over `repo`. Every installed hook stamped with a `# managed by jarvy — [git_hooks.native]` marker so subsequent runs can safely overwrite jarvy's own output; install refuses to overwrite a hook lacking the marker (protects hand-authored `.git/hooks/*`). `jarvy hooks uninstall` removes only marker-bearing files. E2E coverage: `tests/git_hooks_repo_source_e2e.rs` spins up local bare repos in tempdirs and exercises the full clone → scan → install pipeline against real `git` subprocess (`test-bypass` feature; `#[serial]` because tests share process-global env vars).
- `src/library_registry/` — shared library-registry pattern (PRD-054) reused by `[ai_hooks] library_sources`, `[mcp_register] library_sources`, `[skills] library_sources`. Three URL schemes: `https://...` (manifest fetch), `git+https://...@<ref>[#<subpath>]` (PRD-055 git clone, skills-only), `github:owner/repo@<ref>` (shorthand). One manifest format (`{schema_version, publisher, items: [{kind: ai_hook|mcp_server|skill, ...}]}`), HTTPS-bounded fetch, on-disk cache at `~/.jarvy/library.d/<sha256-of-url>/` (+ `git/` subtree for git sources), in-process resolver. Companion artifacts (hook `bash_url`/`powershell_url` bodies, skill `companion_files`) fetch via `companion::fetch_verified` — mandatory manifest sha pin, content-addressed cache at `~/.jarvy/library.d/companions/<sha256>`, no unverified fetch path. Remote-fetched configs CANNOT declare `library_sources` (`library_registry::check_origin` refusal). Cosign sig verify scaffolded but not enforced in v1.
- `src/skills/` — AI agent skill installation (PRD-049 v1 + phase 2). `[skills]` block in `jarvy.toml` + `jarvy skills {install,update,remove,list,status,agents}` CLI. Pulls `SKILL.md` from a library_sources manifest, sha-verifies, writes to `~/.{agent}/skills/<name>/SKILL.md` (claude-code, cursor, codex, windsurf, cline, continue). Per-skill agent narrowing + publisher `supported_agents` filter. `.jarvy-skill.json` sidecar for drift detection. Phase 2: `update` compares sidecar version/sha against the library and reinstalls only on divergence (pinned mismatch refuses); `remove` deletes SKILL.md + sidecar idempotently (user companion files survive); `install <name>` resolves ad-hoc from library_sources at `latest` when the name isn't in `[skills.install]`. Skill names are validated against path traversal (`validate_skill_name`). Still open: skills.sh API, project-scope skills, version-range pinning.
- `src/progress.rs` — spinner abstraction over `indicatif` (PRD-052). Auto-disables on non-TTY, `--quiet`, `--format json`, sandbox / CI, `JARVY_NO_PROGRESS=1`. Used today by `jarvy update check` and `jarvy hooks {install,update}`; further integration follow-up.
- `src/commands/diagnose.rs` — `jarvy diagnose <tool>` extended with Kubernetes cluster liveness probes (Tier 2 preflight). Each k8s tool routes through `check_dependencies` to fast (`--request-timeout=2s`) probes: kubectl → `kubectl cluster-info`; minikube → `minikube status --format={{.Host}}`; kind → `kind get clusters` + shared `kubectl cluster-info`; k3d → `k3d cluster list --no-headers` + shared `kubectl cluster-info`. Advisory (diagnose is read-only); the daemon-preflight pattern from `services::preflight` deliberately doesn't apply here — k8s isn't a runtime dependency for most projects, but exposing the state through `diagnose` gives a fast "why is my cluster not responding?" answer without waiting for the default apiserver 30s retry loop. Probes handle missing CLIs gracefully — `available = false` with details, no crash.
- `src/windows/` — `[windows]` block. Two knobs teams can pick independently to fix "why does my `.sh` script open in Notepad" on Windows: `sh_pathext` (append `.SH` to HKCU PATHEXT so `myscript.sh` runs from cmd/PowerShell) and `sh_association = "off" | "open"` (route the Explorer `.sh` "open" verb to `bash.exe`, leaving "edit" alone so editors still open `.sh` for editing). Both writes are per-user (HKCU) — no admin needed, no impact on other Windows accounts. Bash location auto-detects standard Git for Windows install paths; override via `bash_path_override` (validated for NUL/quote/newline). Parses on every OS (cross-platform teams commit one jarvy.toml); phase short-circuits on non-Windows with `windows.phase_skipped { reason = "not_windows" }`. Trust: remote-fetched configs refused without `allow_remote = true`. Advisory (never fails setup). Pure planners (`pathext::compute_new_pathext`, `sh_association::plan_association`) are cross-platform testable; executors are `cfg(target_os = "windows")` and shell out to `reg add` (PATHEXT: `REG_EXPAND_SZ` to preserve `%VAR%` expansion; association: separate `Jarvy.BashScript` ProgId so a future `remove` can clean up without touching user-authored classes). **`[windows.wsl]` sub-block** — bootstraps Store-WSL2 + a named distro instance, optionally curl-pipes jarvy inside the distro, and (when `run_setup = true`) delegates the outer `jarvy setup` into the distro on the translated project path (`C:\...` -> `/mnt/c/...`). Opt-in via `enabled = true`; `auto_bootstrap = false` default (never triggers UAC — prints elevated `wsl --install -d <distro> --name <name>` command for the user to run). v1 supports base distros `Ubuntu` and `Debian` only. Distro NAME defaults to `distro` verbatim; refuses `docker-desktop` / `docker-desktop-data` at config load. Trust: `[windows.wsl]` inherits `[windows] allow_remote`. Delegation short-circuit lives in `commands/setup_cmd.rs::run_setup` (fires after config load, before tool loop); the WSL sub-phase in `run_wsl_bootstrap_phase` runs alongside `run_windows_phase` when `run_setup = false`. Tool: `src/tools/wsl/` — `define_tool!(WSL, custom_install = bootstrap_wsl, category = "runtime")`. Refuses to touch DISM (legacy pre-Store WSL); refuses to `wsl --unregister` on remove (destructive-action policy: prints the manual command). Rootfs cache at `~/.jarvy/wsl-rootfs/<distro>-<version>-<url-sha[0:16]>.tar.gz` for the `wsl --import` fallback when Store-WSL is too old for `wsl --install --name`. `browser_launcher = "off" | "wslu" | "cmd_shim"` controls how CLIs inside the distro (e.g. `gh auth login`) launch a browser on the Windows host: `"wslu"` installs the `wslu` apt package and sets `$BROWSER=wslview` (Ubuntu-only — `wslu` isn't in Debian's official repos, so `validate()` refuses `browser_launcher = "wslu"` + `distro = "Debian"` at config-load), `"cmd_shim"` sets `$BROWSER` to a `cmd.exe /c start` shim with no package install, `"off"` (default) does nothing. Pure logic (argv builders, path translator, decision seam) is cross-platform testable; executors are `cfg(target_os = "windows")`.
- `src/dotfiles.rs` — `[dotfiles]` block for cross-machine dotfile sync. `manager` = `chezmoi` / `yadm` / `stow`, `repo` (URL or shorthand), `apply` (default true), `allow_remote` (trust gate, default false). Setup phase runs `chezmoi init --apply <repo>` (first run) → `chezmoi update` (subsequent) OR `yadm clone <repo>` → `yadm pull`. `stow` is installed but NOT auto-applied — the per-package model doesn't fit a single verb; the phase runner prints a hint. Remote configs (`jarvy setup --from <url>`) refused without `allow_remote = true` — mirrors `[git_hooks] allow_remote` / `[packages] allow_remote`. Advisory (setup never fails on dotfiles errors). Tool defs for chezmoi/yadm/stow live under `src/tools/{chezmoi,yadm,stow}/`.
- `src/discover/` — project tool auto-discovery (PRD-044 MVP). `jarvy discover [--apply] [--missing] [--format json]` scans the project root for marker files (Cargo.toml, package.json, go.mod, Dockerfile, k8s/, *.tf, Makefile, Justfile, …), infers versions from `rust-toolchain.toml` / `.nvmrc` / `.python-version` / `go.mod`, and either prints suggestions or merges them into `jarvy.toml`. Suggestions are validated against `tools::registry::registered_tool_names()` so we never recommend a tool jarvy can't install. Built-in rules only in v1 (custom rules file deferred).
- `src/maintenance/` — tool freshness advisory (PRD-057). `[maintenance]` block + `jarvy check-updates` cmd. Compares installed `[provisioner]` / `[cargo]` / `[npm]` / `[pip]` / `[gem]` / `[go]` tools against upstream latest via package-manager-native shell-outs (`brew info --json=v2`, `cargo search`, `npm view`, `apt-cache policy`, `dnf info`, `pip index versions`, `gem search -re`, `go list -m -versions -json`, `winget show`, `choco search`, `scoop info`). Backend router picks per-OS: brew (macOS), apt→dnf→brew fallback (Linux), winget→choco→scoop (Windows). `npm ls -g --json --depth=0` batches installed-version lookups for globals in one subprocess. Concurrent-refresher guard via `~/.jarvy/update-cache.lock` — 1h stale-reclaim; RAII drops the file on refresher exit. NEVER blocks setup — `run_maintenance_phase` in `setup_cmd.rs` reads `~/.jarvy/update-cache.json` (sub-millisecond) to print the summary line, then spawns a detached `jarvy check-updates --refresh --background` child via `process_group(0)` (Unix) / `DETACHED_PROCESS | CREATE_NO_WINDOW` (Windows) so the cache warms for the NEXT invocation. First-ever setup shows no summary (empty cache); run #2 onward shows it. Trust gates: `check_updates = false` opts out, `JARVY_CHECK_UPDATES=0` kill-switch, sandbox/CI/non-TTY skip the *spawn* only. Remote configs need `[maintenance] allow_remote = true` to broaden trust (mirrors `[packages]`/`[git_hooks]`/`[dotfiles]`). Version managers (rustup/nvm/pyenv/…) and `custom_install` tools land in the `unchecked` bucket — no false-positive stale flags for tools jarvy doesn't own. PRD-060 Phase 2: a valid install receipt (`~/.jarvy/install-receipts.json`, stat-signature validity) reroutes a tool to its fallback ecosystem's probe — receipt beats BOTH the custom_install bucket and the provisioner backend; receipt package strings are hostile input and pass `receipt_package_safe` (path-shape refusal + `validate_package_name` + go-path safety) or land in `unchecked` as `refused_unsafe_name`. `UvBackend` probes PyPI's JSON API via `net::bounded_fetch` (uv dry-run output is not a stable contract) and serves ONLY receipt-routed targets — no `[uv]` config section, no `uv tool list` batching. `jarvy upgrade` is receipt-aware: receipt-routed tools upgrade through their ecosystem route; `--native` skips the receipt, upgrades via the platform manager, and deletes the receipt on success. Backend timeout = 5s per probe (`tools::common::probe_with_timeout`). Cache TTL default 24h (successes) / 1h (errors) so a transient network blip doesn't wedge the check. Cache key is `{backend}:{pkg_id}` — a route change alters both parts, so no route field is needed for correct invalidation.
- `src/agent_profiles/` — AI agent profile switcher (PRD-058). `jarvy agents profile {init,create,use,save,restore,list,status,delete,check-cwd}` (handler: `commands/agents_profile_cmd.rs`) snapshots agent config dirs into `~/.jarvy/agent-profiles/<profile>/<agent-slug>/` (store 0700 with chmod verified by read-back; file modes preserved so credentials stay 0600; symlinks copied as links, never followed; sockets/FIFOs/device nodes skipped — `fs::copy` returns ENOTSUP on `~/.codex/ipc/ipc.sock` and aborted the whole snapshot). `exclude.rs` holds a per-agent DENYLIST of non-identity paths (transcripts, re-fetchable package/extension/marketplace trees, log DBs, scratch) — measured 2.3 GB → 2.9 MB on a real machine. Denylist not allowlist so an unknown new file is kept, never silently dropped. Which copies filter is a type, `store::CopyPolicy`, not a convention: `Snapshot(&[patterns])` leaves the source authoritative so skipping is safe, while `Relocate` (the EXDEV fallback behind a failed `fs::rename`) deletes the source afterwards and therefore filters nothing and vets no symlinks — a skip there would destroy user data, not merely omit it. `create --from` clones per agent (`clone_profile_tree`) rather than flat-copying: a symlink-tier snapshot arrives by `fs::rename` and was never filtered, so a flat clone duplicated cursor's whole config dir. Cursor's `extensions` tree is deliberately NOT on its denylist — cursor's snapshot *is* the live dir, so a profile without extensions is an editor with no extensions and no way back. `--agents <slugs>` on `init` narrows the set so the env tier can be captured while a symlink-tier editor is still running; like `use`, a narrowed run does NOT claim `default_profile` when one already exists (it only speaks for the agents it named). Registry `profiles.json` — schema-versioned, atomic tmp-write + rename + 0600 (same shape as `maintenance/cache.rs`). Two switching tiers via `Agent::profile_mechanisms()`: **env** (claude-code → `CLAUDE_CONFIG_DIR`, codex → `CODEX_HOME`; `use` prints bash `export` lines, `--print-env` = stdout purity for `eval` — human text → stderr, paths escaped `" \ $ \``) and **symlink** (cursor, windsurf, cline, continue — all promoted to switchable; `--global` re-points `~/.{slug}` atomically via sibling temp link + rename-over, Windows junction fallback `cmd /c mklink /J`). `save [name]` refreshes snapshots from live (env-tier copy; symlink-tier no-op because the live dir IS the snapshot); `restore <name>` is the inverse and targets the DEFAULT config dir so a fresh shell picks it up. Refusals: real dir at the agent path = `UnmanagedDir` (`symlink_metadata`, never follow — agent reinstalled itself), target canonicalizing outside `$HOME` = `EscapesHome`, delete while a live symlink or registry entry points into the profile = `ActiveProfileDelete`. Running-IDE probe in `probe.rs` (`pgrep -x` unix / `tasklist /FI` windows, 1s cap, fail-safe false) refuses symlink-tier swaps for cursor/windsurf when the editor is running unless `--force` is passed — emits `agent_profile.ide_running { agent, forced }`. Cline/Continue skip the probe entirely (in-editor extensions have no separate binary). In `cargo test` the probe short-circuits false via cfg(test); the `JARVY_MOCK_RUNNING_IDES=<slug>[,<slug>]` hook exercises the guard without spawning an editor. `switcher::apply_symlink_repoint` emits NO telemetry — the CLI handler owns all `agent_profile.*` events so `init_snapshot`'s link-back doesn't false-fire a switched event. `jarvy shell-init` snippet also defines `jp` (bare = list, `jp <name>` evals `use --print-env`; nushell `def --env`) and wires a `cd` hook (bash `PROMPT_COMMAND` / zsh `chpwd_functions` / fish `--on-variable PWD`) that fires `jarvy agents profile check-cwd`. That subcommand walks up from cwd for `jarvy.toml`, compares live profile to `[agents.profiles] prefer`, prints one stderr line on divergence, and debounces per (session, repo) for 4h via a 0600 state file at `~/.jarvy/agent-profiles/.cwd-hint-state.json`. `JARVY_NO_CWD_HINT=1` opts out. `cwd_hint::run` never emits the profile name or the repo path — event fields are `matched` + `debounced` only. Ticket bundles exclude `agent-profiles/` (`ticket::collector::TICKET_EXCLUDED_DIRS`) and the ticket env allowlist omits `CLAUDE_CONFIG_DIR`/`CODEX_HOME`. Remote configs: `[agents.profiles]` is stripped in `Config::mark_remote` (narrow-only trust). Profile names NEVER appear in telemetry — counts and bounded agent slugs only. v1 limitation: `jarvy skills install` / `mcp register` write to the agent's default config path, not the env-redirected profile of the current terminal; git-backed sync between machines is v1.1.
- `src/workspace.rs` + `src/commands/workspace_cmd.rs` — monorepo support (PRD-047). `[workspace] members = [...]` declared in root `jarvy.toml`; per-member jarvy.toml inherits via `merge_configs` (provisioner table merges tool-by-tool with member winning). `jarvy workspace {list,show,validate}` is the read-only CLI surface — workspace-aware `jarvy setup --project <name>` orchestration is deferred. Empty `inherit = []` is treated as `["provisioner"]` for the show/list output so the common case "just works".
- `src/update/` — self-updater. Detects install method (brew/cargo/apt/dnf/pacman/winget/choco/scoop/binary). Env: `JARVY_UPDATE`, `JARVY_UPDATE_CHANNEL`, `JARVY_PINNED_VERSION`. CI auto-disables.
- `src/observability/` — log-config types (`LogConfig`/`LogLevel`/`LogFormat`), the `jarvy setup --profile` phase `Profiler`, sanitizer (`Cow<'_, str>` no-alloc on no-match), and `telemetry_gate`. The actual tracing-subscriber wiring (console/file/OTLP layers, CLI log-flag overrides) lives in `src/analytics.rs::init_logging`. `jarvy setup -q/-v/-vv/-vvv/--log-format/--log-file/--debug-filter/--profile` are wired through `main.rs` → `commands/dispatch.rs`: CLI verbosity filters the **console** layer only (not the registry `EnvFilter`, so `-q` doesn't starve `jarvy.log`/OTLP); `--debug-filter`/`-v` widen the registry filter and beat `RUST_LOG`. Rolling appender (`tracing_appender::rolling` daily) + `non_blocking` writers with `WorkerGuard`s held in `analytics::{FILE_LOGGER_GUARD, EXTRA_LOG_GUARD}`; `shutdown_logging()` flushes `SdkLoggerProvider` + both guards before exit. (The former `bundle`/`network_trace` modules were deleted — `DiagnosticBundle` duplicated `src/ticket/`.)
- `src/logging/` — thin re-export over `observability/` + log-file helpers (`read_recent_logs`, `get_log_stats`, `clean_logs`)
- `src/ticket/` — debug-bundle ZIP for support
- `src/remote.rs` — remote config fetch + caching (`fetch_remote_config`, `transform_github_url`)
- `src/interactive.rs` — menu when run without subcommand

### Tool Implementation (`define_tool!` macro)

Each tool lives in `src/tools/{name}/{mod.rs, {name}.rs}`. Minimal example:

```rust
define_tool!(JQ, {
    command: "jq",
    macos: { brew: "jq" },
    linux: { uniform: "jq" },
    windows: { winget: "jqlang.jq" },
});
```

Macro slots: `macos.brew` / `macos.cask`, `linux.uniform` OR `linux.{apt,dnf,pacman,apk}`, `windows.winget` / `windows.choco`, `custom_install: Some(fn)` for shell-script installs (nvm/rustup/brew), `fallback: { go|npm|cargo|uv: "<first-party pkg>" }` (PRD-060 — ecosystem routes tried in declared order when no platform slot covers the OS; missing toolchains auto-bootstrap through jarvy's own registry; ecosystem package managers ONLY, never curl|sh), `default_hook: { description, script }`, `depends_on: &[...]` (strict — all required, applies on every platform), `depends_on_by_os: &[(Os::Windows, "vcredist")]` (strict — applies only when the current OS matches; merged with `depends_on` at check + topo-sort time so a Windows-only prereq doesn't false-fire on macOS/Linux), `depends_on_one_of: &[...]` (flexible — one required), `category: "messaging" | …`. Register in `src/tools/mod.rs::register_all()`.

**Non-obvious rules:**

- **Omit unsupported platforms.** No first-party `winget` / `choco` package on Windows? OMIT the block. Placeholder ids (e.g. `Pivotal.RabbitMQ`) create supply-chain exposure — anyone claiming the publisher namespace can ship a malicious installer that `winget install -e --id` pins to. Runtime emits `tool.unsupported` (routed via `InstallError::is_no_platform_installer()`) — that's the UX. Add a one-line `// No first-party winget manifest as of YYYY-MM; install from <upstream-url>.` per the `kaf`/`kafkactl`/`emqx`/`kn` examples.
- **Dash ↔ underscore aliasing.** `define_tool!(NATS_SERVER, …)` stringifies as `"NATS_SERVER"` (`stringify!` preserves case), which `get_tool_spec` lowercases into `"nats_server"` at map-insert time. `registry::get_tool()` and `get_tool_spec` both fall back to a `-` ↔ `_` swap so `nats-server = "latest"` also resolves. `validate::validate_tools` mirrors it. The bridging primitive `tools::fallback::canonical_key` routes ROUTES / BLOCKED / FAILED_ROUTES / receipt writes through the same lowercase-spec form so `install_route` telemetry, receipt lookup, and phase-2 receipt-based routing agree regardless of config spelling. Pin whichever form upstream docs use; users get the other free.
- **Brew tap auto-tap.** When `macos.brew` (or `linux.brew` fallback) is `org/tap/formula` (exactly two slashes), install path runs `brew tap org/tap` first so a fresh box doesn't surface an "untrusted tap" error. Soft-fail; already-added tap doesn't block.
- **Categories.** Set `category: "messaging" | "workflow" | …` so `tool.installed` carries the label. Operators can graph "what fraction of NATS rollouts succeeded?" without pivoting on tool name. Introduce new category once 3+ tools qualify; backfill opportunistically.
- **Default hooks** are idempotent (scripts check before mutating), advisory (failures = warnings), overridable (user `[hooks.tool]` wins).
- **Dependencies** affect install order via topo sort. Strict missing = warn + install anyway. Flexible: if one in config, install first; if one already present, satisfied; else advisory.

### Trust Boundaries (cross-cutting)

Remote-fetched configs (loaded via `jarvy setup --from <url>`) are tagged `ConfigOrigin::Remote` (see `Config::mark_remote()`). They may **NARROW** trust but cannot **BROADEN** it:

- `[ai_hooks] allow_custom_commands = true` — refused for remote configs; library hooks always allowed.
- `[mcp_register] allow_custom_servers = true` — refused for remote configs; built-in `jarvy` server always allowed.
- `[packages] allow_remote = true` — without this, remote configs cannot install `[npm]/[pip]/[cargo]/[nuget]/[gem]/[go]` entries.
- `[windows] allow_remote = true` — without this, remote configs cannot modify HKCU PATHEXT or `.sh` file association. Prevents a friendly-looking remote config from routing a Windows user's `.sh` files to an attacker binary via `bash_path_override`.
- Project-config telemetry overrides (endpoint) — refused with stderr warning. Endpoint override only via `JARVY_OTLP_ENDPOINT`.

Package-name validation (`validate_package_name` / `validate_package_version`) refuses leading-`-`, URL schemes, shell-meta, and control bytes (ESC/BEL/DEL/NUL — closes ANSI injection in dry-run preview). `jarvy validate` runs them on every `[npm]/[pip]/[cargo]/[nuget]/[gem]/[go]` entry.

### Config Files

- `jarvy.toml` (project) — tools, packages, roles, hooks, etc.
- `~/.jarvy/jarvy.toml` (personal overlay, optional) — SAME schema as a project `jarvy.toml`, auto-merged into every project on load so personal tools/skills/hooks/MCP servers/git identity follow you across machines. Merge semantics mirror workspace inheritance: every top-level section is inherited, project wins on collision, `[provisioner]` merges tool-by-tool. Tagged `ConfigOrigin::Local` — remote-config trust gates do NOT apply, so personal `library_sources` and `allow_custom_*` are honored. Opt out with `JARVY_NO_PERSONAL_CONFIG=1` (CI runners, integration tests). Invalid TOML in the overlay logs `config.personal_overlay_parse_failed` and falls through — never bricks a project load. Applied inside `config::apply_personal_overlay` and wired into both `Config::new` and `Config::new_with_workspace`.
- `~/.jarvy/config.toml` (global runtime) — telemetry, update, machine fingerprint. DIFFERENT file from the overlay above; different schema.
- `.jarvy/state.json` (project) — drift baseline

### Bootstrap script

`scripts/bootstrap.sh` is the canonical one-command onboarding entry point. Reusable: end-user repos can copy it into their own `scripts/` so contributors run `./scripts/bootstrap.sh` to install Jarvy (via `dist/scripts/install.sh`) and execute `jarvy setup` against the repo-root `jarvy.toml`. Idempotent. Flags: `--no-setup`, `--channel <stable|beta|nightly>`, passthrough args to `jarvy setup`. When recommending Jarvy integration to an end user, prefer pointing them at this script over hand-rolled curl-pipe + `cargo install` snippets.

### Logs & Tickets

- Logs: `~/.jarvy/logs/jarvy.log` (+ `.1.gz`, …). Daily rotation, `[logging]` section in `jarvy.toml` controls level/format/retention.
- Tickets: `~/.jarvy/tickets/JARVY-YYYYMMDD-xxxxxxxx.zip`. `jarvy ticket {create,show,list,clean}`.

## Telemetry

OTEL-based, **opt-out by default**. Config in `~/.jarvy/config.toml::[telemetry]` (`enabled`, `endpoint`, `protocol`, `logs`, `metrics`, `traces`, `sample_rate`). Env overrides: `JARVY_TELEMETRY` (`0` disables, `1` enables), `JARVY_OTLP_ENDPOINT`, `JARVY_OTLP_PROTOCOL`, `JARVY_OTLP_{LOGS,METRICS,TRACES}`, `JARVY_OTLP_SAMPLE_RATE`. CI / unattended sandboxes auto-disable unless `JARVY_TELEMETRY=1`. CLI: `jarvy telemetry {status,enable,disable,set-endpoint,test,preview}`. Module: `src/telemetry.rs`.

### Event Taxonomy — stable contract for AI agents and log queries

| Event | Source | Notes |
|-------|--------|-------|
| `tool.requested` | per tool in config | Setup phase |
| `tool.installed` | per successful install | Setup phase. Carries `install_route` (`"platform"` \| `"fallback_go"` \| `"fallback_npm"` \| `"fallback_cargo"` \| `"fallback_uv"`) + `toolchain_bootstrapped` (bool), populated by querying `tools::fallback::route_for` internally so call sites keep their signatures (PRD-060). `install_route` is also an OTEL counter attr. |
| `tool.failed` | per failed install | Setup phase. Carries `install_route` (`"platform"` \| `"fallback_go"` \| `"fallback_npm"` \| `"fallback_cargo"` \| `"fallback_uv"`) populated by `tools::fallback::failed_route_for` (EP-8) so operators can distinguish "brew broke" from "npm fallback broke" without joining against `tool.installed` events that never fired. |
| `tool.unsupported` | setup unknown-tool loop + `--request` + `jarvy diagnose <name>` + `jarvy doctor --tools <name>` + `jarvy search <name>` | TWO field shapes by source. **Wide** (`config` / `request`): `tool, version?, source, platform, suggestions, channel, fallback_issue_url, scaffold_cmd, exit_code, opt_in_bypassed` via `tools::unsupported::build_report`. **Narrow probe** (`diagnose` / `doctor` / `search`) via `telemetry::emit_tool_unsupported_probe`: `tool, source, platform`. All narrow-probe sources go through `sanitize_for_display` (control-byte / bidi / length cap ≤ MAX_TOOL_NAME_LEN) at both the tracing event AND the OTLP counter attribute — the counter's `tool` label is cardinality-bounded regardless of user input. Narrow-probe emissions are `info` level (routine user outcome, not a system fault) and gated behind the telemetry consent gate; `request` bypasses the gate (explicit user action) and stays `warn`. `search` fires when the query is non-empty, `--all` is not set, AND the top result's name does not contain the query as a substring — deliberately stricter than "zero results" because jaro-winkler-only matches don't count as the user finding what they meant. `doctor` dedups by lowercased name so `--tools foo,foo` fires one event, not three. `version` is NOT carried on the counter attribute for narrow-probe sources — it would blow cardinality without adding decision-support value (declarative version demand lives in `[maintenance]` telemetry instead). The registered-but-no-platform-installer sites additionally carry `fallback_declared` (bool) + `fallback_blocked` (`"toolchain_uninstallable"` \| `"none_declared"`) — fires only after all declared fallback routes are exhausted (PRD-060). |
| `tool.fallback_failed` | fallback route blocked on its toolchain (PRD-060) | `tool`, `eco` (`go`\|`npm`\|`cargo`\|`uv`), `error_kind = "toolchain_unavailable"`, `cause` (bounded `InstallError::kind()` label: `"no_platform_installer"` \| `"prereq_missing"` \| `"permission_required"` \| … — added EP-5 so a single failed toolchain bootstrap memoized in `BOOTSTRAP_FAILED` reports its root cause the N-1 later times the same eco is asked). Warn. Gated. Actual install-command failures surface as the returned error, not this event. |
| `tool.receipt_write_failed` | best-effort install-receipt write returned Err (PRD-060) | `tool`, `error_kind = "no_home" \| "io" \| "json"`. Warn. Gated. Receipts at `~/.jarvy/install-receipts.json` (doctor-cache pattern: schema version, atomic 0600, stat signature). Writers = fallback install + receipt-aware `jarvy upgrade` refresh (`--native` deletes the receipt after a native install succeeds). Readers (`receipts::load_valid`) treat stat-mismatched entries as absent but NEVER prune the file. A valid receipt wins routing over both the custom_install bucket and the provisioner backend in `maintenance/resolver.rs`; unknown route strings fall through to platform routing. |
| `tool.distribution_selected` | java installer picked a distribution AND is about to run the route (fires from `Decision::RunRoute` only, not the fallback/refuse paths) | `tool = "java"`, `distribution` (bounded 6-slug set: `openjdk`/`temurin`/`zulu`/`corretto`/`liberica`/`microsoft-openjdk`), `version_kind` (bounded: `"latest"` / `"major"`), `os` (bounded `env::consts::OS`). Info level. Gated. Adoption funnel numerator for "which JDK distributions do users pin?" |
| `tool.distribution_fallback` | java installer refused or fell back the requested distribution — fires on `Decision::FallbackToDefault` (`fell_back = true`) AND `Decision::Refuse` (`fell_back = false`) so both outcomes are graphable together | `tool = "java"`, `parsed_kind` (bounded: `"known"` / `"unknown"` — whether the requested string parsed to a `JdkDistribution`), `fell_back` (bool), `reason_kind` (bounded: `"unknown_distribution"` / `"unsupported_pm"` / `"unsupported_os"` / `"msopenjdk_apt_manual"` / `"exact_fallback_unavailable"`), `os`. `hint` field carries the free-text human-readable reason for log-only debugging (bounded in practice — reasons are static format templates). Warn level. Gated. The raw user-supplied distribution string is NEVER emitted as a metric field to keep cardinality bounded. |
| `maintenance.refused_unsafe_name` | resolver refused a hostile package identifier — TOML ecosystem key or install-receipt package string that fails `validate_package_name` / go-path safety | `purpose = "[cargo]" \| "[npm]" \| "[pip]" \| "[gem]" \| "[nuget]" \| "[maintenance-go]" \| "[receipt]"`, `reason = "path_shaped_key" \| "traversal_segment" \| "receipt_package_invalid"`. NO tool field — the name is attacker-controllable. Refused entries land in the `unchecked` bucket as `refused_unsafe_name`. Warn. Gated. |
| `tool.already_installed` | skip-detection path | `install_path`, `detection_method`, `prompted_user` |
| `setup.started` / `setup.completed` | run lifecycle | Carries duration, counts |
| `hook.started` / `hook.completed` / `hook.failed` / `hook.timeout` | per hook | |
| `packages.phase_started` / `packages.phase_completed` | run_packages_phase | Carries `dry_run`, `npm`, `pip`, `cargo`, `nuget`, `gem`, `go` booleans, `duration_ms` |
| `packages.phase_skipped` | early-return | `reason`, `dry_run` |
| `packages.phase_previewed` | dry-run preview | Per-ecosystem `*_count` (renamed from `packages.dry_run`) |
| `packages.remote_refused` | trust gate refusal | `reason` (e.g. `allow_remote_packages_not_set`) |
| `packages.install_failed` | install_packages outer error | One per ecosystem on failure |
| `package.requested` | per-package entry | `ecosystem`, `package`, `version`, `platform` |
| `package.installed` | per-package success | + `duration_ms` |
| `package.failed` | per-package failure | `error!` level; `error` redacted |
| `package_command.failed` | run_package_command non-zero | + `stderr_tail` (last 4KB) |
| `commands.extras_refused_keys` | `[commands]` key sanitizer (`config::sanitize_extras_keys` — shared by interactive menu AND `jarvy run` via `read_commands_config`) | `count` (control-byte / Trojan-Source keys dropped). Gated. |
| `run.command.start` / `run.command.complete` / `run.command.failed` | `jarvy run <name>` (npm-run-style `[commands]` runner) | `label` (display-sanitized, same field name as `interactive.command.*` so the two domains join), `cmd_hash` (shared `short_cmd_hash` — command text never emitted), `role` (bounded: `"main"` / `"pre_hook"` / `"post_hook"` — so hook-vs-main funnels for e.g. `pre:android → android` in the mobile template are graphable without string-parsing `label`); start adds `well_known` (run/test/setup slot vs extra), `extra_args_count`; complete adds `exit_code` (`-1` sentinel = signal-killed, matches interactive; process exit stays 1), `duration_ms`; failed adds `error` |
| `run.command.not_found` | `jarvy run <name>` with no matching `[commands]` entry | `label`. Warn. |
| `run.command.refused` | `jarvy run` refused execution | `label`, `reason = "nul_byte" \| "percent_windows"` (cmd.exe expands `%VAR%` even inside quotes — no escape exists, so `%`-bearing `--` args are refused on Windows). Warn. |
| `run.command.config_error` | `jarvy run` couldn't load jarvy.toml | `error_kind = "missing" \| "unreadable" \| "parse"`. Warn. |
| `run.command.list` | bare `jarvy run` (listing mode) | `format`, `count` — execute-vs-discovery split. |
| `interactive.command.start` / `interactive.command.complete` / `interactive.command.failed` / `interactive.command.refused` | interactive menu selecting a `[commands]` entry (`src/interactive.rs::run_shell_command`) | `label` (bounded — `run`/`test`/`setup`/extra key name), `cmd_hash` (`short_cmd_hash`, command text never emitted); complete adds `exit_code` (`-1` = signal-killed) + `duration_ms`; failed adds `error`; refused adds `reason`. Info / Warn (refused, failed). Gated (EP-2). |
| `interactive.command.declined` | user declined the security confirm prompt — default-No Enter, explicit `n`, and Esc all land here | `label`, `cmd_hash`. Info. Gated (EP-6). Denominator for the confirm-accept rate metric. |
| `interactive.first_run.choice` | first-run onboarding — user picked one of the four options | `choice = "quickstart" \| "init" \| "templates" \| "skip"` (bounded label — NEVER the raw menu string, which could carry any future UI copy). Info. Gated (EP-6). Adoption funnel numerator. |
| `interactive.menu.cancelled` | user hit Esc / interrupted the returning-user OR first-run menu | `context = "main" \| "first_run"`. Info. Gated (EP-6). Distinguishes abandonment from selection. |
| `shell_init.started` | `jarvy shell-init` invoked (before shell detection) | none — jarvy-owned INFO witness for the WarnOnly-cap `-v` escape hatch test. |
| `shell_init.generated` | `jarvy shell-init` emitted a snippet | `shell` (bash/zsh/fish/sh/powershell/nushell) — per-shell adoption. |
| `env.rc.extend_emitted` | Shell rc writer emitted an extend statement (`export PATH="new${PATH:+:$PATH}"` and per-shell equivalents) — the `append = true` / `PathPosition` feature adoption signal. Fires from `update_rc_content` only; `preview_shell_rc` is silent (dry render, no real write) | `shell` (bounded: `"bash"` / `"zsh"` / `"sh"` / `"fish"` / `"nushell"` / `"powershell"` via `ShellType::as_slug`), `position` (bounded: `"prepend"` / `"append"`), `key_class` (bounded 8-label taxonomy: `"path"` / `"manpath"` / `"infopath"` / `"cdpath"` / `"classpath"` / `"suffix_path"` / `"suffix_paths"` / `"suffix_dirs"` / `"other"` via `classify_env_key`). Info. Gated. The raw env-var key is NEVER emitted — user-defined `*_PATH` names are unbounded cardinality. |
| `ensure.completed` | `jarvy ensure` finished foreground work successfully | `mode = "foreground" \| "background"`, `invocation_source = "rc_snippet" \| "manual"`. Gated. |
| `ensure.failed` | `jarvy ensure` returned non-zero OR background spawn failed | `error_kind`, `error`, `mode`, `invocation_source`. Error level. Fires from BOTH foreground (user-visible via stderr + log-file lead) and background (silent to user; file log is the only trail). Gated. |
| `logs.strip.started` | `jarvy logs clean --filter <PATTERN>` began line-strip | `pattern_kind = "event" \| "substring"`, `all`, `dry_run`. Pattern text NEVER logged (user-authored → PII / cardinality risk). Gated. |
| `logs.strip.completed` | strip finished (or dry-run reported) | `pattern_kind`, `event_name` (only when pattern_kind=event; bounded taxonomy), `all`, `dry_run`, `files_touched`, `lines_removed`, `bytes_saved`, `files_skipped_symlink`, `files_skipped_read_failed`, `files_skipped_too_large`, `duration_ms`. Gated. |
| `logs.strip.failed` | strip returned Err mid-loop | `error_kind = "io"`, `error`. Error level. Gated. |
| `logs.strip.forensic_refused` | `--filter event=<name>` targets a security refusal event (`git_config.*_refused`, `mcp.mutation.wizard_bypass_unexpected_client`, `library.companion.sha_mismatch`, …) without `--allow-forensic-strip` | `event_name`. Warn level. Exit code 3. Gated. |
| `logs.strip.pattern_ambiguous` | pattern starts with `event` but not `event=<name>` — typo trap (`event: foo`, `event.foo`) | `hint`. Warn level. Gated. |
| `completions.generated` | `jarvy completions <shell>` generated a script | `shell` — per-shell adoption. |
| `ai_hook.phase_started` / `ai_hook.phase_completed` | apply phase | `agents`, `applied`, `refused_local`, `refused_remote`, `failures`, `duration_ms` |
| `ai_hook.agent_applied` / `ai_hook.agent_failed` | per agent | `agent_failed` carries `error_type` only — formatted message NOT emitted (no user-content leak) |
| `ai_hook.provisioned` | per hook | `agent`, `hook_name`, `library_source` |
| `ai_hook.custom_refused_summary` | trust gate | `local_count`, `remote_count` — counts only, no names/bodies |
| `ai_hook.check_completed` | drift check | `agents_checked`, `drifted_agents` |
| `ai_hook.windows_auto_translated` | Bash→PowerShell xlat | `agent`, `hook_name` |
| `mcp_register.phase_started` / `mcp_register.phase_completed` | apply phase | `agents`, `applied`, `refused_local`, `refused_remote`, `failures`, `duration_ms` |
| `mcp_register.agent_applied` / `mcp_register.agent_failed` | per agent | `agent_failed`: `error_type` only |
| `mcp_register.auto_detected` | `jarvy setup` synthesizes a default `[mcp_register]` because the project had no block and at least one AI agent was detected on disk | `count`, `agents` (comma-joined slugs), `platform`. Does NOT fire when an explicit `[mcp_register]` block is present, in dry-run, in test mode, in seamless / CI sandboxes, or when `JARVY_MCP_REGISTER=0`. |
| `registry.sync.started` / `registry.sync.completed` | `jarvy registry sync` lifecycle | `registry_url` (redacted), `require_signature`, `tools_synced`, `tools_removed`, `signature_verified`, `duration_ms` |
| `registry.sync.failed` | preflight + per-stage error returns | `stage = "preflight" \| "manifest_parse" \| ...`, `reason` (when preflight), `error` |
| `registry.sync.signature_refused` | cosign verification rejected | `registry_url`, `identity_regexp`, `oidc_issuer`, `reason` |
| `registry.signature_disabled` | `require_signature = false` escape hatch | `registry_url` |
| `registry.sync.sha_mismatch` | per-tool sha verification fails | `tool`, `worker_id`, `url` (redacted), `expected`, `actual` |
| `registry.sync.tool.start` / `registry.sync.tool.synced` | per-tool in parallel loop (debug) | `tool`, `worker_id`, `bytes` |
| `registry.sync.tool_fetch_failed` / `tool_parse_failed` / `tool_write_failed` | per-tool failures inside the parallel loop | `tool`, `worker_id`, plus `url`/`error` |
| `registry.fetch.start` / `registry.fetch.completed` | per HTTPS GET in `fetch_bounded` (debug) | `url`, `max_bytes`, `bytes` |
| `registry.fetch.failed` | non-200 / size cap / network err | `url`, `registry_url`, `error` |
| `registry.cache.swap_failed` | atomic-swap of `tools.new/` into `tools/` failed | `stage = "promote" \| "rollback"`, `error` |
| `registry.cache.index_built` | parsed-tools index written | `accepted_count` |
| `registry.cache.index_build_failed` | index write IO failure (non-fatal) | `error` |
| `registry.cache.index_perms_unsafe` | `index.json` mode looser than `0700` after chmod | `mode` |
| `registry.cache.index_hit` / `registry.cache.index_miss` | plugin loader read the index cache (debug) | `tools_count`, `synced_at_unix` / `reason` |
| `registry.cli.sync_failed` | `jarvy registry sync` exit-code mapping | bounded `error_kind` label only |
| `signature.skipped` / `signature.verified` / `signature.failed` | cosign verify path (was `update.signature.*` — renamed because shared with registry) | `file`, plus `reason` (skipped) or `error` (failed) |
| `git_hooks.phase_started` / `git_hooks.phase_completed` | setup `run_git_hooks_phase` (PRD-048) | `dry_run`, `installed`, `duration_ms` |
| `git_hooks.phase_skipped` | no framework detected | `reason` |
| `git_hooks.phase_previewed` | dry-run preview | `framework` |
| `git_hooks.remote_refused` | remote config tried auto_install | `reason = "allow_remote_not_set"` |
| `git_hooks.install_failed` | install_hooks returned error | `error_kind`, `error` |
| `git_hooks.installed` / `git_hooks.updated` | per-framework op | `framework = "native" \| "husky" \| "lefthook"` (pre-commit removed in v0.8); native adds `count` (resolved hook count), `scanned_dir` (bool — was a `[git_hooks.native] dir` set?), `scanned_repo` (bool — was a `[git_hooks.native] repo` set?), `explicit_count` (u64 — entries in `[git_hooks.native.hooks]` map). |
| `git_hooks.mutable_ref` | `[git_hooks.native] ref` looks like a branch (not a SHA / `v`-prefixed tag) — publishers can silently rev the hook body | `git_ref`. Warn. Gated. Advisory only; install proceeds. Mirrors `library.git.mutable_ref`. |
| `git_hooks.install_started` / `git_hooks.install_completed` | `install_hooks()` entry/exit (CLI + setup callers; obs P1 review item 23/24) | started: `enabled`, `auto_update`, `run_after_install`. completed: `status = "applied" \| "skipped" \| "failed"`, `applied`, `framework`, `auto_update`, `run_after_install`, `duration_ms` |
| `git_hooks.update_started` / `git_hooks.update_completed` | `update_hooks()` entry/exit | started: `enabled`. completed: `status`, `applied`, `framework`, `duration_ms` |
| `jdk.vendor_repo.bootstrap_started` | Linux vendor-repo bootstrap begins (curl GPG key → gpg --dearmor → sudo tee) | `vendor` (bounded: `"Adoptium Temurin"` / `"Azul Zulu"` / `"Amazon Corretto"` / `"BellSoft Liberica"` / `"Microsoft OpenJDK"`), `pm` (`"apt"` / `"dnf"`). Info. Gated. |
| `jdk.vendor_repo.bootstrapped` | Bootstrap finished successfully — GPG key installed, sources.list / repo file written, `apt-get update` / `makecache` complete | `vendor`, `pm`, `duration_ms` (u64). Info. Gated. |
| `jdk.vendor_repo.bootstrap_skipped` | 30-day mtime idempotency hit — no work needed | `vendor`, `pm`, `reason = "recent_mtime"`. Debug. Gated. |
| `jdk.vendor_repo.bootstrap_failed` | Bootstrap failed at a specific stage — `stage` field is the failure attribution on-call needs | `vendor`, `pm`, `stage` (bounded: `"require_curl"` / `"require_gpg"` / `"require_lsb_release"` / `"resolve_codename"` / `"codename_refused"` / `"mkdir_key_parent"` / `"fetch_key"` / `"fingerprint_mismatch"` / `"write_sources_list"` / `"write_repo_file"` / `"apt_update"` / `"makecache"`), `error` (redacted subprocess error line). Warn. Gated. |
| `jdk.vendor_repo.fingerprint_check_skipped` | Bootstrap ran without GPG fingerprint verification because `expected_fingerprint == "UNVERIFIED"` (Azul / Corretto / BellSoft as of Aug 2026 — TODO(security) verify against vendor docs) | `vendor`. Warn. Gated. |
| `git_config.phase_started` / `git_config.phase_completed` | setup `run_git_phase` | started: `remote`, `scope`. completed: `ok`, `error_kind` (bounded: `git_not_installed`/`refused`/`write_failed`/`io`/`invalid_key`/…/`none`), `remote`, `scope`, `signing`, `aliases`, `extra_key_count`, `os_defaults_enabled`, `duration_ms`. |
| `git_config.phase_previewed` | dry-run git phase | `remote`, `scope`, `os_defaults_enabled`, `extra_key_count`. |
| `git_config.remote_refused` | remote-origin config applied `[git]` without `allow_remote` | `reason = "allow_remote_not_set"`. Warn. |
| `git_config.extra_applied` | `[git.extra]` keys written (summary) | `key_count`, `sections` (comma-joined DISTINCT section prefixes — never values/full keys). |
| `git_config.extra_key_applied` | per `[git.extra]` key, AFTER its write (partial-failure trail) | `section` (prefix only). Debug. |
| `git_config.os_defaults_applied` | `configure_os_defaults` (both branches — opt-out graphable) | `enabled`, `opted_out`, `keys_written`. |
| `git_config.os_default_key_applied` | per default key, AFTER its write succeeds | `key`, `value` (both jarvy constants). Debug. |
| `git_config.exec_key_refused` / `git_config.exec_key_override_applied` | `[git.extra]` key whose value git executes — refused, or applied under `JARVY_ALLOW_GIT_EXEC_KEYS` | `key` (never value). Warn. |
| `git_config.exec_value_refused` | typed `editor`/`credential_helper` (shell-interpreted key) value with a `!` / shell metacharacter / program path | `key`. Warn. |
| `git_config.protect_downgrade_refused` / `git_config.protect_downgrade_override_applied` | `[git.extra]` value weakens a guardrail — refused, or applied under `JARVY_ALLOW_GIT_PROTECT_DOWNGRADE` | `key`, `guardrail = "protect_ntfs_hfs" \| "safe_directory_wildcard" \| "safe_bare_repository" \| "fsck_objects_disabled" \| "fsck_ignore" \| "tls_verify_disabled"`. Warn. |
| `git_config.shell_escape_refused` / `git_config.shell_alias_refused` | `!`-shell value / alias refused | `key` / `alias`. Warn. |
| `git_config.refused_invalid_key` / `git_config.refused_option_value` | `[git.extra]` key grammar / leading-`-` value refused | `key`. Warn. |
| `git_config.set_failed` | `git config` write exited non-zero | `key`, `error_brief` (first line of git stderr, ≤160 chars). Warn. |
| `library.sync.started` | per-source fetch begins (PRD-054) | `url` (redacted), `require_signature` |
| `library.sync.completed` | fetch + parse OK | `url`, `items_synced`, `ai_hook_count`, `mcp_server_count`, `skill_count`, `from_cache`, `signature_verified` |
| `library.fetch.cached_hit` | served from disk cache after network failure | `url`, `reason = "fetch_failed"` |
| `library.cache.write_failed` | disk-write best-effort failure (non-fatal) | `url`, `error` |
| `library.signature_disabled` | `require_signature = false` warning | `url` |
| `library.remote_refused` | trust gate — remote config declared `library_sources` | `consumer = "ai_hooks" \| "mcp_register" \| "skills"`, `reason` |
| `skills.installed` | per-skill install success | `skill`, `version`, `agent_count`, `skipped_count` |
| `skills.updated` | per-skill `jarvy skills update` success (PRD-049 phase 2) | `skill`, `version`, `updated_count`, `unchanged_count`, `skipped_count`. Emitted even when everything was unchanged (`updated_count = 0`) so no-op runs are graphable. |
| `skills.removed` | per-skill `jarvy skills remove` success | `skill`, `removed_count`, `absent_count`. Idempotent re-removes emit with `removed_count = 0`. |
| `library.git.clone_started` | begin git clone (PRD-055) | `repo` (redacted), `git_ref` |
| `library.git.clone_completed` | clone + SKILL.md walk succeeded | `repo`, `git_ref`, `subpath`, `skills_discovered`, `duration_ms` |
| `library.git.clone_failed` | `git` subprocess exit nonzero | `args` (per-arg redacted — review item 6), `exit`, `error` (redacted) |
| `library.git.cache_hit` | served from clone cache after git failure | `url`, `reason = "git_failed"` |
| `library.git.mutable_ref` | branch ref pinned (publishers can rev silently) | `repo`, `git_ref`, `advice` |
| `library.git.missing_git` | `git` CLI not on PATH | `os` |
| `library.git.symlink_skipped` | symlink in cloned repo refused (review item 2) | `path` (relative to clone root) |
| `library.git.path_escape_refused` | SKILL.md canonicalizes outside clone root (review item 2 defense-in-depth) | `canon_path` |
| `library.git_skill.skipped` | SKILL.md missing required frontmatter | `path`, `reason` (missing name / version / parse fail) |
| `library.file_url_refused` | `file://` URL points outside cache root (review item 3) | `reason = "outside_cache_root"` |
| `library.sync.failed` | every sync error path (review item 8) | `url`, `scheme = "manifest" \| "git"`, `error_kind`, `error` |
| `library.companion.fetched` | companion artifact fetched + sha-verified (hook `bash_url` / `powershell_url` bodies, skill `companion_files`) | `url` (redacted), `bytes`, `from_cache`. Debug level on content-addressed cache hits, info on fresh fetches. |
| `library.companion.sha_mismatch` | fetched companion body doesn't match the manifest sha pin | `url`, `expected`, `actual`. Error level. |
| `library.companion.fetch_failed` | every other companion fetch error path (invalid pin, network, non-HTTPS, non-UTF-8 script body) | `url`, `error_kind`, `error`. Warn level. |
| `library.companion.refused_filename` | skill `companion_files` filename failed path-safety validation (traversal / absolute / control bytes / jarvy-owned name) | `skill`, `reason` (filename NOT logged — it's attacker-controllable, mirrors `workspace.member_invalid`). Warn level. |
| `library.signature.verified` | `require_signature = true` + cosign-verified successfully (PRD-054 phase 5) | `url`. Per-fetch — cached hits don't re-emit. |
| `template.materialized` | a built-in or user template was rendered into a `jarvy.toml` — fires from BOTH `jarvy templates use <name>` and `jarvy init --template <name>` so cross-entry adoption is queryable; init path was added alongside the mobile Android/RN/Expo overhaul so the two entry points can be time-sliced separately (`source = "init"` cohort measures the mobile bet). | `template`, `category`, `tool_count`, `source` (bounded: `"builtin"` = templates-use path, `"init"` = init path), `output_path` (or `<stdout>`). Info level. Gated for the init path; the templates-use path emits unconditionally (pre-existing). Profile names / raw user content NEVER emitted — `template` is the bounded template slug. |
| `discover.applied` | `jarvy discover --apply` succeeded (PRD-044) | `tools_added`, `recommended_added`, `already_configured`, `recommended_dropped_dup` (count of companions suppressed because also required), `detections_by_rule` (comma-joined rule names), `target = "merged" \| "noop" \| "bailed_to_fresh"`, `duration_ms` |
| `discover.setup_advisory` | `jarvy setup` ran the continuous-discovery scan and found new tools (PRD-044 phase 2) | `new_tools`, `uninstallable` |
| `discover.recommended_dropped` | per-companion suppression when the same tool is required via own-marker (dedup dashboard trail) | `name`, `promoted_to = "required"`. Debug level. |
| `discover.rules_loaded` | one-shot per process at first `discover` invocation — count of loaded detection rules | `default_rule_count`, `custom_rule_count`, `total_rule_count`. Info level. Guards against `#[cfg]` regressions silently dropping rules. |
| `discover.jarvy_toml_perms_unsafe` | chmod on discover-written `jarvy.toml` failed or was silently ignored (NFS/drvfs/exFAT) | `target`, `error` OR `mode`, `fs_hint = "chmod_failed" \| "chmod_ignored"`. Warn level. |
| `discover.sensitive_key_refused` | discover refused to write a config carrying a top-level `[secrets]` / `[credentials]` / `[tokens]` / `[api_keys]` / `[auth]` section — case-insensitive | `key` (original case), `key_lower`. Error level — invariant breach on the 0644 chmod policy. |
| `dotfiles.phase_started` | `[dotfiles]` phase begins in setup | `manager = "chezmoi" \| "yadm" \| "stow"`, `apply` (bool). Info. Gated. |
| `dotfiles.phase_completed` | phase finished successfully (Applied or NoOp) | `manager`, `apply_kind = "initial_clone" \| "update" \| "git_pull_only" \| "pull" \| "noop"` (separates adoption from retention — a fresh `initial_clone` on a shared box is a security-relevant event, a nightly `update` is not), `apply_mode = "auto_apply" \| "clone_only"` (sizes the high-intent security-conscious segment that reviews before applying), `duration_ms`. Info. Gated. |
| `dotfiles.phase_failed` | phase failed | `manager`, `apply_mode`, `duration_ms`, `error_kind = "auth" \| "network" \| "not_found" \| "conflict" \| "binary_missing" \| "permission_denied" \| "spawn_failed" \| "other"`. **NEVER emit raw subprocess stderr as an event field** — git/chezmoi echo the repo URL (with any embedded token) on auth failures. `PhaseOutcome::Failed` carries `error_kind` (bounded, telemetry-safe) and `error` (human message; caller-only). Warn. Gated. |
| `dotfiles.phase_skipped` | trust-gate-clean skip paths — dry-run, empty repo, stow (manual-only), or the manager CLI missing from PATH | `manager`, `reason = "dry_run" \| "empty_repo" \| "stow_manual" \| "manager_not_installed"`. Info. Gated. |
| `dotfiles.phase_absent` | `[dotfiles]` block NOT present in project config — denominator for the adoption-rate funnel | (no fields). Debug. Gated. |
| `dotfiles.remote_refused` | trust gate refused a remote-origin config's `[dotfiles]` without `allow_remote`, OR input-safety refused a leading-`-` / NUL-bearing `repo` (CVE-2017-1000117-class argv-injection into git via `--upload-pack=/tmp/pwn`) | `manager`, `reason = "allow_remote_not_set" \| "invalid_repo"`. Warn. Gated. Emitted by `emit_refused`; `apply_chezmoi` / `apply_yadm` also scrub GIT_*/CHEZMOI_* code-execution env vars via `dotfile_command` before spawning. |
| `windows.phase_started` / `windows.phase_completed` | `[windows]` phase begins / finishes on Windows targets | started: `sh_pathext` (bool), `sh_association` (`"off"` / `"open"`), `has_override` (bool — `bash_path_override` set). completed: `pathext` (`"applied"` / `"unchanged"` / `"failed"` / `"skipped"`), `association` (same set), `duration_ms`. Info. Gated. |
| `windows.phase_skipped` | phase declined to run | `reason = "disabled" \| "not_windows" \| "nothing_to_do" \| "remote_refused"`. Info. Gated. `nothing_to_do` distinguishes "block present but no knob enabled" from `disabled` — matters for adoption funnels. |
| `windows.remote_refused` | remote-origin config declared `[windows]` without `allow_remote = true` | `reason = "allow_remote_not_set"`. Warn. Gated. Mirrors `[dotfiles]` / `[git_hooks]` remote-refused shape. |
| `windows.pathext_applied` / `windows.pathext_unchanged` | HKCU PATHEXT write outcome (Windows-only) | `extension = ".SH"` (bounded — only extension jarvy manages today). Info. Gated. |
| `windows.pathext_failed` | `reg add HKCU\Environment\PATHEXT` returned non-zero | `error` (redacted subprocess error). Warn. Gated. |
| `windows.sh_association_applied` / `windows.sh_association_unchanged` | `.sh` association write outcome (Windows-only) | `program` (resolved bash.exe path — bounded via `DEFAULT_BASH_PATHS` or the user's explicit `bash_path_override`, which is length-safe / char-safe from `validate_bash_path`). Info. Gated. |
| `windows.sh_association_failed` | `reg add HKCU\Software\Classes\...` returned non-zero, OR bash.exe couldn't be located | `error` (bounded `AssociationError` variant display). Warn. Gated. |
| `wsl.probe_completed` | end of a WSL probe (`wsl --version` + `wsl -l -q` + optional `which jarvy`) | `store_based` (bool), `distros_installed` (u64), `target_distro_present` (bool), `jarvy_in_distro` (bool). Info. Gated. NO instance-name field — user-supplied names are unbounded cardinality. |
| `wsl.bootstrap_refused` | bootstrap decided not to run | `reason` (bounded: `"not_opted_in"` / `"not_store_wsl"` / `"no_wsl_command"` / `"auto_bootstrap_off"` / `"not_elevated"` / `"reserved_name"`). Warn. Gated. Every refusal also prints a copy-pasteable next-step command to stderr via `render_refusal`. |
| `wsl.bootstrap_started` / `wsl.bootstrap_completed` / `wsl.bootstrap_failed` | `wsl --install --name` or `wsl --import` outcome | started: `base_distro` (bounded: `"Ubuntu"` / `"Debian"`). completed: `base_distro`, `method = "wsl_install" \| "wsl_import"`, `duration_ms`. failed: `base_distro`, `method`, `cause = "exit_nonzero" \| "spawn" \| "rootfs_download" \| "location_invalid"`, `error` (bounded subprocess error line). Info / Warn (failed). Gated. |
| `wsl.jarvy_install_started` / `wsl.jarvy_install_completed` / `wsl.jarvy_install_failed` | curl-pipe of `dist/scripts/install.sh` inside the WSL distro | started: `base_distro`, `channel` (bounded: `"stable"` / `"beta"` / `"nightly"`). completed: `base_distro`, `channel`, `duration_ms`. failed: `base_distro`, `channel`, `cause = "exit_nonzero" \| "network" \| "spawn"`, `error`. Info / Warn (failed). Gated. Fires only when `[windows.wsl] install_jarvy = true` and jarvy is NOT already on PATH inside the distro. |
| `wsl.setup_delegated` | `wsl -d <name> -- bash -lc "cd '/mnt/c/...' && jarvy setup"` returned. Fires from the delegation short-circuit in `run_setup` when `run_setup = true` | `base_distro`, `exit_code` (signal-killed reports `-1`), `duration_ms`. Info. Gated. The inner jarvy inside the distro emits its own `tool.installed` / `setup.completed` events independently — this event is the outer-side attribution. |
| `wsl.path_refused` | cwd path for delegation couldn't be translated to `/mnt/<drive>/...`, OR remote-config trust gate refused the whole block | `reason = "unc" \| "invalid_chars" \| "remote_refused"`. Warn. Gated. Path itself is NOT emitted — it may carry the user's HOME (which on Windows is the account name). |
| `wsl.remove_refused` | `jarvy tools --remove wsl` refused; jarvy never `wsl --unregister`s a distro automatically | no fields; the name is not emitted. Warn. Gated. Destructive-action policy — prints the exact `wsl --unregister <name>` command to stderr for the user to run manually. Exit code 1. |
| `wsl.browser_launcher_configured` | apt-get install (wslu mode only) + idempotent `~/.bashrc` export line succeeded inside the distro | `launcher = "wslu" \| "cmd_shim"`, `duration_ms`. Info. Gated. |
| `wsl.browser_launcher_failed` | apt-get install or `~/.bashrc` append returned nonzero / spawn failed | `launcher`, `cause = "apt_install_failed" \| "rc_write_failed"`, `error` (bounded subprocess error line). Warn. Gated. Fires only when `[windows.wsl] browser_launcher` is not `"off"`. |
| `config.personal_overlay_applied` | `~/.jarvy/jarvy.toml` overlay merged into a project's config on load | `section_count`, `sections` (bounded, comma-joined TOP_LEVEL_SECTIONS names — cardinality safe). **Debug level** (was Info — fires on every jarvy invocation, would flood log budgets). `path` field REMOVED — carried the user's HOME which on macOS/Windows is the account name (PII-adjacent). Gated. |
| `config.personal_overlay_parse_failed` | overlay file exists but is invalid TOML — falls through to project as-is | `error_kind = "toml_syntax"` (bounded — raw `toml::de::Error` display is NEVER interpolated; its output includes source-file line context, which combined with a hostile symlink at the overlay path would leak file contents). Warn level. Gated. |
| `config.personal_overlay_refused` | overlay refused on safety grounds — symlink, foreign owner, world-writable, or oversize (>1 MiB DoS cap) | `reason = "symlink" \| "foreign_owner" \| "world_writable" \| "too_large"`. `path` NOT emitted. Warn level. Gated. Because trust in the overlay is implicit-by-location (~/.jarvy is inherently "your machine"), these gates are load-bearing — they're what makes the trust invariant safe. |
| `config.personal_overlay_disabled` | overlay skipped via `JARVY_NO_PERSONAL_CONFIG=1` env-var opt-out — friction/adoption signal | `reason = "env_opt_out"`. Debug level. Gated. |
| `config.env.append_refused` | `validate_env_append_keys` refused a config load — user set `append = true` on a non-PATH-like key (e.g. `EDITOR = { value = "vim", append = true }` or `[env.rustc.vars] JAVA_HOME = { append = true }`). Fires from BOTH `Config::new` and `Config::new_with_workspace` before `process::exit(CONFIG_ERROR)` | `context` (bounded: `"top_level"` / `"per_tool"`). Warn. Gated. The offending key is NEVER emitted — user-authored. |
| `services.daemon_check` | container-runtime backend preflight (Docker Compose + Podman Compose) — probes `docker info` / `podman info` before `compose up` | `backend = "docker" \| "podman"`, `state = Running \| Down \| Missing \| Timeout`, `duration_ms`. **Info level** (was debug; default prod log filters drop debug, silently killing the Docker-vs-Podman adoption signal). Gated. |
| `services.daemon_down` | preflight returned Down/Missing/Timeout — daemon binary present but socket not reachable | `backend`, `state = Down \| Missing \| Timeout`, `hint_kind = "colima" \| "docker_desktop_macos" \| "docker_desktop_windows" \| "wsl_docker_desktop" \| "systemd" \| "podman_machine" \| "podman_socket"` (bounded — answers "colima vs Docker Desktop, which suggestion do users see most?"), `duration_ms`. Warn level. Gated. |
| `services.daemon_probe_timeout` | wall-clock cap `PROBE_TIMEOUT` fired — subprocess was killed. Emitted IN ADDITION TO `services.daemon_down { state = Timeout }` so on-call can graph "hung daemon" separately from "daemon exited fast non-zero" | `backend`, `timeout_ms`, `duration_ms`. Warn level. Gated. |
| `services.status_daemon_check_swallowed` | `status()` observed a non-`DaemonNotRunning` error from `check_daemon()` and refused to run `compose ps` against an unknown daemon state | `backend`. Warn level. Gated. Forensic breadcrumb — the silent-fallback pattern before this was the classic outage vector. |
| `diagnose.tool_probed` | k8s liveness probe fired from `jarvy diagnose <tool>` (kubectl / minikube / kind / k3d) | `tool`, `state = "reachable" \| "unreachable" \| "binary_missing" \| "permission_denied" \| "timeout" \| "spawn_failed"`, `cluster_count` (kind/k3d only), `probe_ms`, `hit_timeout` (bool — probe took ≥ K8S_PROBE_TIMEOUT). Info level. Gated. Every probe now goes through `tools::common::probe_with_timeout` — the "hard 2-second budget" the diagnose doc comment claims is actually enforced (previously only kubectl honored `--request-timeout=2s`; minikube/kind/k3d hung indefinitely). |
| `doctor.probe_cache` | end of `jarvy doctor` tool pass — disk-backed version-probe cache summary (`~/.jarvy/doctor-cache.json`, `src/commands/doctor_cache.rs`) | `hits`, `misses`, `fresh` (bool — `--fresh` bypassed reads). Validity is stat-based (binary path+mtime+size), so a tool upgrade invalidates its entry with no TTL window; negative probes (no parseable version / binary missing) expire after 1h. Info. Gated. |
| `doctor.probe_cache_read_failed` / `doctor.probe_cache_write_failed` | best-effort cache I/O returned Err — doctor continues with empty in-memory cache | `error_kind = "no_home" \| "io" \| "json"`. Warn. Gated. |
| `services.backend_selected` | per-invocation attribution — which backend `detect_backend` picked for the current jarvy run | `backend = "docker" \| "podman" \| "tilt"`, `docker_installed`, `podman_installed`, `podman_variant = "podman_compose_builtin" \| "podman_compose_standalone" \| "podman_compose_missing" \| "n/a"`, `source = "detect" \| "compose_file_override"`. Info level. Gated. Enables `podman_selected / total_selected` adoption funnel. |
| `workspace.validate_completed` | `jarvy workspace validate` finished (PRD-047) | `status = "ok" \| "warnings" \| "invalid"`, `members`, `errors`, `warnings`, `duration_ms`. `warn!` level when `errors > 0`, else `info!` |
| `workspace.member_invalid` | per-member validation failure | `error_kind = "escapes_workspace_root" \| "dir_missing" \| "toml_parse_fail"` (member name NOT logged — it's attacker-controllable in a hostile root config) |
| `wizard.started` | `jarvy wizard` start (PRD-056) | `mode = "headless" \| "skill_drop" \| "quickstart_fallback"`, `agent`, `apply`, `skill_only` |
| `wizard.skill_dropped` | wizard wrote a SKILL.md to the agent's skills dir | `agent`, `skill_path` |
| `wizard.headless_spawned` | wizard spawned the agent's CLI in headless mode | `agent`, `cmd_argv0` (argv[0] only — args carry the prompt body), `mcp_preapproval` (allowlist scope passed via `--allowedTools`, or `""` when none), `wizard_session_env = true` (marker that `JARVY_WIZARD_SESSION=1` was set on the child), `wizard_session_id` (per-invocation UUID for correlation) |
| `wizard.headless_exit` | agent CLI returned | `agent`, `exit_code`, `wall_ms`, `jarvy_toml_before = "absent" \| "present"`, `jarvy_toml_after = "absent" \| "created" \| "unchanged" \| "modified"`, `terminal_state = "playbook_completed" \| "noop_already_configured" \| "early_exit" \| "unknown"`, `wizard_session_id` |
| `wizard.refused` | trust-boundary or runtime refusal | `reason = "sandbox" \| "ci" \| "non_tty" \| "remote_config" \| "no_agent_installed" \| "skill_drop_failed" \| "headless_spawn_failed"` |
| `wizard.session_token_activate_failed` | `WizardSessionGuard::activate` couldn't write the marker file (perms error, disk full, read-only FS) — wizard runs on with a no-op guard | `error`, `path`. Warn level. |
| `wizard.session_token_perms_unsafe` | marker file chmod to 0600 failed OR was silently ignored — mirrors the `discover.jarvy_toml_perms_unsafe` pattern | `path`, `error` OR `mode`, `fs_hint = "chmod_failed" \| "chmod_ignored"`. Warn level. Marker's 0600 is a capability boundary — silent chmod ignore lets other local users forge tokens. |
| `wizard.session.bypass_refused` | `session::is_active()` returned false while gating an MCP mutation — distinguishes "user typed no" from "orphaned descendant of a killed wizard tried to bypass" | `reason = "env_missing" \| "env_empty" \| "no_home" \| "marker_missing" \| "mtime_unavailable" \| "marker_stale" \| "marker_future_mtime"`, `session_id`. Debug level. |
| `mcp.mutation.wizard_bypass` | wizard-driven MCP mutation skipped the TTY confirmation (see `src/mcp/extended_tools.rs::gate_mutation`) | `tool`, `client` (`claude-code` \| `codex` only — unexpected clients fall through), `workspace`, `effect` (ANSI-sanitized), `pid`, `wizard_session_id`. Info level. |
| `mcp.mutation.wizard_bypass_unexpected_client` | env + marker present but MCP client is NOT `claude-code` / `codex` — bypass does NOT fire; forensic warning of a compromised same-user MCP client | `tool`, `client`, `workspace`, `wizard_session_id`. Warn level. |
| `maintenance.phase_started` / `maintenance.phase_completed` | `jarvy check-updates` (foreground/background) + `jarvy setup` cache-read (PRD-057) | started: `mode = "cli_foreground" \| "cli_background" \| "setup_read"`, `tool_count`, `cache_ttl_hours`. completed: `mode`, `tools_checked`, `updates_available`, `unchecked`, `errors`, `duration_ms`. Gated. |
| `maintenance.phase_skipped` | phase declined to run | `reason = "disabled_by_config" \| "disabled_by_env" \| "sandbox" \| "ci" \| "non_tty" \| "no_provisioner_tools" \| "cache_fresh" \| "remote_refused"`. Info. Gated. |
| `maintenance.remote_refused` | `ConfigOrigin::Remote` config tried to enable `check_updates` without `[maintenance] allow_remote = true` | `reason = "allow_remote_not_set"`. Warn. Gated. Mirrors `[packages] allow_remote` / `[git_hooks] allow_remote`. |
| `maintenance.background_spawned` | `jarvy setup` successfully forked a detached `check-updates --refresh --background` child so the cache warms for the NEXT invocation without blocking THIS one | `pid`. Info. Gated. |
| `maintenance.background_spawn_failed` | fork/exec of the background refresher failed — advisory only, setup completes normally | `error_kind = "exec" \| "permission_denied" \| "io" \| "current_exe"`. Warn. Gated. |
| `maintenance.cache_read_failed` / `maintenance.cache_write_failed` | best-effort cache I/O returned Err — empty cache still usable | `error_kind = "no_home" \| "io" \| "json"`. Warn. Gated. |
| `agent_profile.created` / `agent_profile.deleted` | `jarvy agents profile init/create/delete` (PRD-058) | created: `action = "init" \| "create"`, `seeded_from_existing` (bool — `create --from`), `agents_filtered` (bool — `--agents` narrowed the run; separates "user has one agent" from "user deliberately scoped"), `duration_ms`, `agent_count`. deleted: `agent_count`. Profile NAMES never emitted. Info. Gated. |
| `agent_profile.switched` | `use` — one event PER AGENT so `mechanism` stays bounded and per-agent attribution survives | `agent` (slug), `mechanism = "env" \| "symlink"`, `scope = "tty" \| "global"` (symlink rows always `"global"`), `duration_ms`, `invocation_source = "rc_snippet" \| "manual"` (from `JARVY_JP_INVOCATION`, set by the shell `jp` functions — same vocabulary as `ensure.*` so the two domains join on this field), `first_switch` (bool — agent had no prior `registry.active` entry, read BEFORE anything switches), `agents_filtered`. Emitted by the CLI handler ONLY — `switcher::apply_symlink_repoint` is silent (init link-back must not false-fire). Symlink rows fire at re-point time, not with the env tier at the end: the live link is already moved, so a later registry-write failure must not erase the record of a switch the user's IDE already sees. Info. Gated. |
| `agent_profile.snapshot_completed` | one per agent snapshot | `agent`, `mode = "copy" \| "move"`, `files_copied`, `bytes`, `subtrees_excluded`, `symlinks_skipped`, `special_skipped`, `duration_ms`. **Info, deliberately** — the per-path skips below are debug and the file/OTLP sinks floor at info, so without this the snapshot is invisible in production. This is also the only way a denylist *miss* is detectable: a rule that stops matching shows up as `subtrees_excluded` collapsing while `bytes` jumps, which per-path events cannot show because they simply stop being emitted. `subtrees_excluded` counts denylist *matches*, not files — an excluded directory is skipped whole, so one match can stand for a gigabyte. A `move` that succeeded via `fs::rename` reports zeros; the EXDEV copy fallback reports real counts. Gated. |
| `agent_profile.snapshot_failed` | one agent's snapshot returned Err — the run aborts here | `agent`, `mode = "copy" \| "move"`, `error_kind` (bounded `io::ErrorKind` label: `"not_found"`, `"permission_denied"`, `"already_exists"`, `"invalid"`, `"unsupported"`, `"out_of_memory"`, `"io"`), `duration_ms`. WHICH agent aborted exists only here: `op_failed` knows the subcommand, not the agent it was on, and a `move` that failed mid-copy has already deleted part of the source. Warn. Gated. |
| `agent_profile.clone_completed` | `create --from` finished cloning a whole profile tree | `files_copied`, `bytes`, `subtrees_excluded`, `symlinks_skipped`, `special_skipped`, `duration_ms`. Deliberately NOT a `snapshot_completed` row with a sentinel agent — this rolls up every agent at once, so filing it under `agent = "<profile>"` would put a value in the `agent` dimension no per-agent query can ever mean. Info. Gated. |
| `agent_profile.op_failed` | any profile CLI action returned `ProfileError` (exit CONFIG_ERROR), including the `ActiveProfileDelete` refusal | `action = "init" \| "create" \| "use" \| "list" \| "status" \| "delete"`, `stage` (bounded step within the action: `"create_profile"`, `"snapshot"`, `"registry_load"`, `"registry_save"`, `"prune"`, `"plan"`, `"symlink_repoint"`, `"format_exports"`, `"load"`, `"report"`, `"delete"`), `error_kind` (bounded `ProfileError` variant names: `"io"`, `"unmanaged_dir"`, `"escapes_home"`, `"profile_not_found"`, `"profile_exists"`, `"agent_not_snapshotted"`, `"active_profile_delete"`). `action` + `stage` are load-bearing together: without them an `error_kind = "io"` row can't distinguish a failed snapshot copy during `init` from a failed registry write during `use`, and only the latter leaves the store half-applied — a `use` that died at `registry_save` has already re-pointed the live symlink. Warn. Gated. |
| `agent_profile.use_noop` | `use` selected symlink-tier agents without `--global` — nothing switched for them, stderr notice printed | `reason = "symlink_tier_needs_global"`, `agents` (comma-joined slugs). Info. Gated. |
| `agent_profile.saved` | `jarvy agents profile save [name]` — env-tier snapshots refreshed from live; symlink-tier is live-in-profile already so counts as no-op | `agent_count` (sum, retained for backwards compat = `env_copied + symlink_noop`), `env_copied` (per-agent env-tier writes), `symlink_noop` (managed symlink-tier agents whose live link already points into the profile), `skipped_count` (per-agent "no active profile — skipping"; CLI prints a stderr note but the run exits 0), `agents_filtered` (bool — `--agents` narrowed), `duration_ms`. Split fields mirror `skills.updated` — collapsing them into one integer hid "half the agents were skipped for reasons" behind "everything ran cleanly". Profile names NEVER emitted. Info. Gated. |
| `agent_profile.restored` | `jarvy agents profile restore <name>` — snapshot copied back over live config dirs (env tier) and re-pointed the symlink tier | `agent_count` (sum = `env_copied + symlink_repointed`), `env_copied`, `symlink_repointed`, `skipped_count` (unfiltered runs silently skip agents lacking a snapshot; the running-IDE guard's per-agent refusals also increment this — explicit `--agents` still errors on a missing snapshot), `agents_filtered`, `duration_ms`. Profile names NEVER emitted. Info. Gated. |
| `agent_profile.ide_running` | symlink-tier swap attempted while the target IDE was running — refused unless `--force` overrode | `agent` (slug, bounded — field name mirrors every other `agent_profile.*` event so cross-event joins work), `forced` (bool). Warn when refused (`forced = false`), Info when overridden. Gated. Fires only for agents with a `desktop_binary_names()` entry (cursor, windsurf); cline/continue are in-editor extensions and skip the check. In `cargo test` the probe returns false unconditionally (cfg(test) short-circuit + `JARVY_MOCK_RUNNING_IDES` mock hook), so the developer's own editor state doesn't leak into unrelated tests. |
| `agent_profile.cwd_hint` | shell `cd` hook (bash `PROMPT_COMMAND` / zsh `chpwd` / fish `--on-variable PWD`) fired `jarvy agents profile check-cwd` for a repo carrying `[agents.profiles] prefer` | `matched` (bool — the live profile equals `prefer`), `debounced = false`, `invocation_source = "rc_snippet" \| "manual"` (from `JARVY_CWD_HINT_INVOCATION`; same vocabulary as `ensure.*` / `switched` so the domains join). Info. Gated. Debounced runs are SILENT (no output, no event) — the state file at `~/.jarvy/agent-profiles/.cwd-hint-state.json` (0600) keys by `sha256(session_id + "|" + repo_root)` with a 4h TTL, so the event rate is bounded by "distinct (session, repo) pairs per TTL" rather than "prompts per TTL". Profile names NEVER emitted; the repo path is also not emitted (`$HOME` under it carries the account name on macOS/Windows). Opt out with `JARVY_NO_CWD_HINT=1`. |
| `agent_profile.not_configured` | `jarvy setup` observed no `agent-profiles/profiles.json` — denominator for the adoption funnel | (no fields). Debug. Gated. |
| `agent_profile.setup_hint` | `jarvy setup` compared the live profile against `[agents.profiles] prefer` | `strict` (bool — `= true` raises a stdout hint to a stderr warning; never fails setup), `matched` (bool), `mismatched_count`, `unmanaged_count`. Profile names NEVER emitted — `matched` is the whole signal. Fires only when `prefer` is set and non-empty. `mismatched` (agent on a *different* profile) is kept distinct from `unmanaged` (agent installed but not profile-managed at all) so the hint doesn't nag every non-adopter. Env-tier agents are read from `CLAUDE_CONFIG_DIR`/`CODEX_HOME`, NOT the registry: `profiles.json` records the last *global* switch while the env var is what this terminal is actually on. No trust gate needed — `[agents.profiles]` is stripped wholesale from remote configs in `mark_remote`. Info. Gated. |
| `agent_profile.unmanaged_dir` | symlink re-point refused over a real directory | `agent`. Warn. Gated. |
| `agent_profile.perms_unsafe` | store-dir chmod 0700 failed or was silently ignored (NFS/drvfs/exFAT) — mirrors `discover.jarvy_toml_perms_unsafe` | `error` OR `mode`, `fs_hint = "chmod_failed" \| "chmod_ignored"`. Warn. Gated. |
| `agent_profile.remote_refused` | remote-origin config carried `[agents.profiles]` — block stripped in `mark_remote` | `reason = "remote_origin"`. Warn. Gated. |
| `agent_profile.agent_absent` | snapshot skipped an agent with no config dir | `agent`. Debug. Gated. |
| `agent_profile.snapshot_cross_device` | move-mode snapshot hit EXDEV — fell back to copy+delete | `agent`; warn variant adds `error_kind = "cleanup_failed"`, `error` when the post-copy source delete failed. Debug (fallback) / Warn (cleanup failure). Gated. |
| `agent_profile.symlink_skipped` | snapshot copy skipped an absolute or tree-escaping symlink (only relative in-tree links are preserved — mirrors `library.git.symlink_skipped`) | `agent`, `reason = "absolute_target" \| "escapes_root" \| "unreadable"` (from `store::symlink_refusal_reason`, purely lexical — the link is never followed). Path NOT emitted: under an agent config dir it carries `$HOME`, which on macOS/Windows is the account name. Warn. Gated. |
| `agent_profile.path_excluded` | recursive copy skipped a path carrying no identity — conversation transcripts, re-fetchable package/extension trees, log DBs, scratch (`src/agent_profiles/exclude.rs`). Denylist, not allowlist: an unrecognized new file is kept (profile is bigger than needed) rather than dropped (switching loses config). Fires under `CopyPolicy::Snapshot` only — that covers env-tier `init`/`create` AND the per-agent filtering of `create --from`. `CopyPolicy::Relocate` (the EXDEV move fallback) never filters: it deletes its source afterwards, so a skip there destroys data rather than leaving it behind | `agent`, `pattern` (the denylist rule that fired — a jarvy constant, so cardinality-safe; without it a rule that stops matching anything can never be retired). Path NOT emitted. Debug. Gated. |
| `agent_profile.special_file_skipped` | snapshot copy skipped a non-regular file. Sockets/FIFOs/device nodes are live IPC endpoints, not config (codex keeps `~/.codex/ipc/ipc.sock`); `fs::copy` returns ENOTSUP on them, which aborted the whole snapshot mid-copy | `agent`, `kind = "socket" \| "fifo" \| "block_device" \| "char_device" \| "other"`, `relocating` (bool). Path NOT emitted — filenames inside an agent config dir are user content. **Warn when `relocating = true`** (the source is about to be deleted, so the file does not survive), Debug otherwise. Gated. |

**Telemetry gate.** Every `library.*`, `library.git.*`, `library.git_skill.*`, `skills.*`, `git_hooks.*`, `package.*`, `discover.*`, `workspace.*`, `wizard.*`, `run.command.*`, `commands.*`, `shell_init.*`, `completions.*`, `ensure.*`, `env.rc.*`, `logs.strip.*`, `services.*` (including backend_selected), `dotfiles.*`, `config.personal_overlay_*`, `config.env.*`, `diagnose.tool_probed`, `jdk.vendor_repo.*`, `maintenance.*`, `agent_profile.*`, `interactive.*` (added EP-2 / EP-6), `tool.distribution_*`, `tool.unsupported` (narrow-probe `diagnose`/`doctor`/`search` sources — routed through `telemetry::emit_tool_unsupported_probe`; `--request` deliberately bypasses per explicit-consent rule), `template.materialized` (init path only; templates-use path pre-existing and unconditional), `windows.*` (phase lifecycle + per-knob outcomes), and `mcp.mutation.*` / `mcp.auto_approve.*` event reads `observability::telemetry_gate::is_enabled()` before emitting. Users with `telemetry.enabled = false` don't ship event breadcrumbs even when the OTLP exporter is otherwise configured. Review item 7 (P0) — previously the new `library.git.*` / `skills.*` / `git_hooks.*` domains bypassed the gate; now consistent with `packages.*`. The `mcp.auto_approve.*` domain was added to this list in the follow-up review's Obs F11 fix. **ALL `git_config.*` events are gated** — lifecycle, adoption, AND the security refusals/overrides — via the module-local `gated_warn!`/`gated_info!`/`gated_debug!` macros in `src/git/setup.rs` (and the inline `if is_enabled()` in `run_git_phase`). Round-2 review corrected an earlier attempt to leave the security refusals ungated "for local audit": `tracing` has no sink split, so ungated = shipped to OTLP regardless — a `telemetry.enabled = false` consent breach. The user still sees every refusal via the returned `Err` → stderr, so gating costs no user-facing signal. Do NOT add a `git_config.*` `tracing::…!` that bypasses the macros.

**`env_kind` companion field.** Every event and OTLP attribute that carries `platform` also carries a bounded `env_kind = "wsl" | "native"`, from `telemetry::env_kind()` (which reads `tools::common::is_wsl()`: a memoized one-time `/proc/version` check on Linux, always false elsewhere). A Linux-target binary reports `platform = "linux"` whether it runs on bare metal or inside a WSL distro; `env_kind` is what tells those apart. The field is purely additive and `platform` is unchanged, so the per-event rows above do not repeat it: this paragraph is the authoritative statement. Covers `tool.*`, `setup.started`, `setup.inventory`, the maintenance probe metrics, the `jarvy.setup` span, the telemetry disclosure events, and `mcp_register.auto_detected`. Cardinality is bounded at two values, so it is safe as a metric attribute.

**`tool.unsupported` fields** — TWO shapes depending on source:

**Wide** (`source = config | request` — setup unknown-tool loop, `tools --request`):
```
tool, version?, source, platform, suggestions, channel,
fallback_issue_url, scaffold_cmd, exit_code, opt_in_bypassed
```

**Narrow probe** (`source = diagnose | doctor | search` — `emit_tool_unsupported_probe`):
```
tool, source, platform
```

- `source`: `config` | `mcp` | `cli` | `request` | `diagnose` | `doctor` | `search`
- `channel`: `telemetry` (canonical) | `manual` (telemetry disabled — user must use `fallback_issue_url`). Wide-shape only.
- `opt_in_bypassed`: `true` only on `--request` (user typed command = implicit consent; OTEL counter fires regardless of global opt-in). `--request` also emits `counter_fired`; when `false`, falls back to `manual` channel. Wide-shape only.
- `fallback_issue_url`: present only when `channel = "manual"`; telemetry-on path omits to keep log lines short. Wide-shape only.
- `exit_code`: always `8` (TOOL_UNSUPPORTED). Setup emits per unknown tool but process exits `8` only when ALL configured tools unknown; mixed runs return `0`. Wide-shape only.
- `version?`: wide-shape only — narrow-probe emissions deliberately drop version to keep the counter's `tool` label cardinality bounded. Declarative version demand is derivable from `[maintenance]` telemetry instead.
- `tool` label sanitization: narrow-probe sources launder the user-typed string through `crate::tools::unsupported::sanitize_for_display` before it reaches EITHER the tracing event OR the OTLP counter attribute — strips ANSI/control bytes/bidi overrides + caps length at `MAX_TOOL_NAME_LEN`. Wide-shape sources go through `build_report`, which uses the same sanitizer. Never emit user input via `%raw`.
- **Metric counter**: `jarvy.tool.unsupported` (renamed from `jarvy.tool.not_supported` to match event name).

**Telemetry gate.** Every `package.*` / `packages.*` / `package_command.failed` event reads `observability::telemetry_gate::is_enabled()` before emitting — populated by `telemetry::init` at startup. Without it, prior implementation leaked package events to OTLP when `telemetry.enabled = false` but an endpoint was set for unrelated reasons. Broke documented opt-in contract.

## Structured output (`--format json`)

Every command that prints to stdout accepts `--format json` (PRD-051). The
JSON is pretty-printed by default and routed through either the canonical
`output::Outputable` trait (for handlers that ship a typed result struct)
or an inline `serde_json::json!()` envelope. CLI exit codes are identical
between human and JSON paths so `jq .` pipelines can rely on `$?` for
control flow. Commands with subcommand actions (`drift`, `logs`, `ticket`,
`services`, `workspace`) carry `--format` on each subcommand rather than
the parent; this matches the existing pattern set by `drift check` /
`logs view` and keeps clap parsing unsurprising.

## Testing

Integration tests in `/tests/`. Env vars:
- `JARVY_TEST_MODE=1` — disables interactive prompts
- `JARVY_FAST_TEST` — skips external command execution

## Exit Codes

- `0` Success
- `2` CONFIG_ERROR (malformed `jarvy.toml`)
- `3` PREREQ_MISSING (package manager not found)
- `4` NETWORK_TIMEOUT (network / proxy failure)
- `5` PERMISSION_REQUIRED (sudo needed)
- `6` INCOMPATIBLE_OS_ARCH
- `7` HOOK_FAILED (pre_setup / post_install / post_setup)
- `8` TOOL_UNSUPPORTED (every configured tool unknown; mixed runs return `0`)

Drift command additionally: `1` drift detected, `2` no baseline.

## Conventions

- Rust 2024 edition
- Conventional Commits: `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`
- Prefer stdlib + existing deps over new crates
- `cargo fmt` + `cargo clippy` before committing
